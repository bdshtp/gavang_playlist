name: Update Gavang Playlist

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests Pillow

      - name: Update Gavang playlist
        run: |
          python update_gavang.py

      - name: Check generated files
        run: |
          echo "===== gavang.m3u ====="
          test -f gavang.m3u
          head -20 gavang.m3u
          echo "======================"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add gavang.m3u logos/gavang/

          if git diff --cached --quiet; then
            echo "Không có thay đổi."
          else
            git commit -m "Update Gavang playlist"
            git push
          fi
name: Update Gavang Playlist

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests Pillow

      - name: Update Gavang playlist
        run: |
          python update_gavang.py

      - name: Check generated files
        run: |
          echo "===== gavang.m3u ====="
          test -f gavang.m3u
          head -20 gavang.m3u
          echo "======================"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add gavang.m3u logos/gavang/

          if git diff --cached --quiet; then
            echo "Không có thay đổi."
          else
            git commit -m "Update Gavang playlist"
            git push
          fi
